import React from 'react';
import styles from './styles';

const TopLeft = () => <i style={styles.topLeft} />;
const TopRight = () => <i style={styles.topRight} />;
const BottomLeft = () => <i style={styles.bottomLeft} />;
const BottomRight = () => <i style={styles.bottomRight} />;

export { TopLeft, TopRight, BottomLeft, BottomRight };
